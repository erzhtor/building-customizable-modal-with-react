# Building a Modal Component with React

This repo was created for the blog post.

## Running

`yarn start` - run in dev mode
`yarn build` - build project
`yarn test` - run tests
`yarn eject` - eject from react-scripts
